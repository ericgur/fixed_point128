# Build output
bin/
build/
out/
__pycache__/
/Testing

# Benchmark results and the baseline the /benchmark skill compares against. Machine specific:
# rates measured on one box say nothing about another, so they are never committed.
bench/results/

# Visual Studio
**/.vs
*.user
*.aps
*.bak

# Doxygen output (regenerated by CI, see .github/workflows/docs.yml)
html

# VS claude code extensions
.claude/settings*.json
.claude/*.ps1
.mcp.json
